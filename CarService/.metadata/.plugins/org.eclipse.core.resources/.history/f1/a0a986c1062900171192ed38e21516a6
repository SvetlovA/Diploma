(function() {
	var indicators = document.querySelectorAll(".indicator"),
		parentNode;
	
	indicators.forEach(function(indicator) {
		parentNode = inicator.parentNode;
		if (parentNode.nodeValue === "Online") {
			indicator.classList.add("online");
		}
	});
})();